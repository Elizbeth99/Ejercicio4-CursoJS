const nombre= "Elizbeth"
const apell= "Romero"

const estudiante= nombre.concat("").concat(apell)

const estudianteMayus= estudiante.toUpperCase()
const estudianteMinus= estudiante.toLowerCase()

const estudianteLength= estudiante.length

const inicialnombre= nombre.substring(0,1)

const finalapell= apell.substring(apell.length-1, apell.length)

const estudiantesinespacio= estudiante.replace(//g,"")

const nombreEnEstudiante = estudiante.includes(nombre)